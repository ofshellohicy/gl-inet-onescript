# OpenClash 原配置地区节点审计（脱敏）

## 范围

- 日期：2026-08-18
- 设备：GL.iNet GL-MT3000
- 文件：`/etc/openclash/config/config.yaml`
- 状态：该文件在检查时不是活动配置
- 敏感信息：未记录订阅地址、节点名、服务器地址、端口、UUID、密码、公钥或短 ID

## YAML 结构

- 文件大小：299,710 字节
- 静态节点：48
- proxy provider：0
- 代理组：2
- 规则：7,999
- 协议计数：HTTP 16、Hysteria2 21、VLESS 11
- 规则策略计数：`Proxy` 7,173、`DIRECT` 826

按节点名称中的旗帜与地区标识统计：

| 国家或地区代码 | 节点数 |
| --- | ---: |
| US | 22 |
| JP | 10 |
| GB | 4 |
| DE | 3 |
| AU | 2 |
| CA | 2 |
| ID | 2 |
| IN | 2 |
| FR | 1 |
| HK / MO / TW / SG | 0 |

两个代理组内也未发现香港、澳门、台湾或新加坡节点引用。

## OpenClash 订阅元数据

- 订阅启用：是
- 配置名：`config`
- 订阅转换：`sub_convert=0`
- 自定义排除词：`香港`、`台湾`、`奈飞`、`新加坡`
- 默认排除词：未设置
- 订阅地址：一个直接 HTTPS 地址，无查询参数；地址及令牌未记录

## 源码核对

路由器当前 `/usr/share/openclash/openclash.sh` 会读取 `ex_keyword`，但仅在 `sub_convert=1` 且存在转换模板时，将排除正则编码进转换请求的 `exclude` 参数。`sub_convert=0` 分支直接使用原订阅地址。

因此当前 `ex_keyword` 不是本次 YAML 无四地区节点的充分原因。澳门也不在已存排除词中。

## 结论

- 已证实：当前 `config.yaml` 及两个代理组均没有香港、澳门、台湾、新加坡节点。
- 未证实：未来更新仍会排除四地区。
- 配置缺口：澳门没有持久排除词；现有排除词在 `sub_convert=0` 下不参与直接订阅下载。
- 本次为只读检查，未修改路由器。
