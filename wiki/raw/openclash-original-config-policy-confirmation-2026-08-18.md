# OpenClash 原配置排除策略确认

## 日期

2026-08-18

## 用户确认

- 原 `config.yaml` 当前配置可按现状使用，验收结论为 OK。
- 预期排除香港、澳门、台湾、新加坡节点。
- `奈飞` 是有意保留的排除关键词，用于排除名称带奈飞标识的节点，不是误配置。
- 本次不要求修改订阅、关键词或 OpenClash 运行状态。

## 维护口径

- 将上述内容记录为策略意图和当前验收决定。
- 保留技术审计边界：当前 YAML 符合预期，但订阅更新后仍需重新统计节点；该边界不构成立即修复要求。
- “排除奈飞节点”不等于阻止客户端访问 Netflix 服务。
